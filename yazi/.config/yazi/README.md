# Updating Theme

Note that the theme is generated automatically. After breaking changes, the `script/template.toml` has to be updated. The updated theme can be generated by running:

```bash
node yazi/.config/yazi/script/generate-flavor.js
```

All colors are defined in that `.js` file.
