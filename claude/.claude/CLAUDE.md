# Global CLAUDE.md

## Git & Moving Files

Note that Git operations are disabled and any attempt to run `git mv` will therefore fail. When moving files inside a project directory, use the plain `mv` bash command instead.
